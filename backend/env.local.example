# ============================================
# ORIONX BACKEND - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in actual values
# This combines values from env.example.cloud and cloud deployment settings

# ============================================
# DATABASE (Supabase)
# ============================================
# Get from: Supabase Dashboard > Project Settings > Database > Connection String
# Format: postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres?sslmode=require
SUPABASE_DB_URL=postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres?sslmode=require

# Fallback for local development (if SUPABASE_DB_URL not set)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/orionx

# ============================================
# REDIS (Upstash)
# ============================================
# Get from: Upstash Dashboard > Redis Database > REST API
# Format: redis://default:[PASSWORD]@[ENDPOINT].upstash.io:[PORT]
REDIS_URL=redis://default:[PASSWORD]@[ENDPOINT].upstash.io:[PORT]

# Fallback for local development (if REDIS_URL not set)
UPSTASH_REDIS_URL=redis://localhost:6379/0

# ============================================
# AI (OpenRouter)
# ============================================
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-[YOUR_API_KEY]

# ============================================
# SECURITY
# ============================================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=[GENERATE_A_RANDOM_32_CHAR_STRING]

# ============================================
# SUPABASE (Additional Keys)
# ============================================
# Get from: Supabase Dashboard > Project Settings > API
SUPABASE_SERVICE_ROLE_KEY=[YOUR_SUPABASE_SERVICE_ROLE_KEY]
SUPABASE_PROJECT_URL=https://[PROJECT_REF].supabase.co
SUPABASE_ANON_KEY=[YOUR_SUPABASE_ANON_KEY]

# ============================================
# CORS
# ============================================
# Production: Allow all origins for cloud deployment
CORS_ORIGINS=*

# Optional: Specific frontend domain
FRONTEND_DOMAIN=app.orionx.ai

# ============================================
# RAILWAY AUTO-PROVIDED
# ============================================
# PORT is automatically set by Railway - do NOT set manually
# PORT=8000  # Only for local development

# ============================================
# NOTES
# ============================================
# 1. For Railway deployment, set these in Railway Dashboard > Variables
# 2. For local development, fill in actual values or use defaults
# 3. Never commit actual secrets to Git
# 4. Replace [PASSWORD], [PROJECT_REF], [ENDPOINT], [PORT], [YOUR_API_KEY] with actual values
# 5. To create .env: Copy this file to .env (cp env.local.example .env)

