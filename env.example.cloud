# ============================================
# ORIONX CLOUD DEPLOYMENT - ENVIRONMENT VARIABLES
# ============================================
# Copy this file and fill in your actual values
# DO NOT commit actual secrets to version control

# ============================================
# SUPABASE DATABASE (PostgreSQL + pgvector)
# ============================================
# Get from: Supabase Dashboard > Project Settings > Database > Connection String
# Format: postgresql+asyncpg://postgres:[PASSWORD]@[HOST]:5432/postgres
SUPABASE_DB_URL=postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres

# Supabase Project URL (for frontend)
SUPABASE_PROJECT_URL=https://[PROJECT_REF].supabase.co
SUPABASE_ANON_KEY=[YOUR_SUPABASE_ANON_KEY]
SUPABASE_SERVICE_ROLE_KEY=[YOUR_SUPABASE_SERVICE_ROLE_KEY]

# ============================================
# UPSTASH REDIS
# ============================================
# Get from: Upstash Dashboard > Redis Database > REST API
# Format: redis://default:[PASSWORD]@[ENDPOINT]:[PORT]
UPSTASH_REDIS_URL=redis://default:[PASSWORD]@[ENDPOINT].upstash.io:[PORT]

# ============================================
# OPENROUTER API (AI - DeepSeek R1)
# ============================================
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-[YOUR_API_KEY]

# ============================================
# RAILWAY BACKEND CONFIGURATION
# ============================================
# Railway automatically sets PORT, no need to configure
# Add these in Railway Dashboard > Service > Variables:

# Database
SUPABASE_DB_URL=[SAME_AS_ABOVE]

# Redis
UPSTASH_REDIS_URL=[SAME_AS_ABOVE]

# AI
OPENROUTER_API_KEY=[SAME_AS_ABOVE]

# Security
SECRET_KEY=[GENERATE_A_RANDOM_SECRET_KEY_FOR_JWT]

# CORS (optional - for custom domain)
FRONTEND_DOMAIN=app.orionx.ai
VERCEL_URL=[AUTO_SET_BY_VERCEL]

# ============================================
# VERCEL FRONTEND CONFIGURATION
# ============================================
# Add these in Vercel Dashboard > Project > Settings > Environment Variables:

# Backend API URL (Railway)
NEXT_PUBLIC_API_URL=https://[YOUR_RAILWAY_APP].railway.app

# WebSocket URL (Railway - use wss:// for secure)
NEXT_PUBLIC_WS_URL=wss://[YOUR_RAILWAY_APP].railway.app

# Supabase (for direct client access if needed)
NEXT_PUBLIC_SUPABASE_URL=https://[PROJECT_REF].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=[YOUR_SUPABASE_ANON_KEY]

# ============================================
# OPTIONAL: RATE LIMITING
# ============================================
RATE_LIMIT_SECRET=[GENERATE_A_RANDOM_SECRET_FOR_RATE_LIMITING]

# ============================================
# NOTES
# ============================================
# 1. Railway automatically provides PORT environment variable
# 2. Vercel automatically provides VERCEL_URL environment variable
# 3. Never commit actual secrets to Git
# 4. Use Railway and Vercel's environment variable management UI
# 5. For production, enable HTTPS and use secure cookies

